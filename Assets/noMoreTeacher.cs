using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class noMoreTeacher : MonoBehaviour
{
    // Start is called before the first frame update
    public void end()
    {
        this.gameObject.SetActive(false);
    }
}
